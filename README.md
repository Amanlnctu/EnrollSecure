# EnrollSecure
EnrollSecure is a Python-based student login system designed to restrict access to computers. Upon startup, the application prompts students to enter their enrollment number. If the entered enrollment number is valid, access is granted. The system also records the login time in a CSV file.
